<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quizzine - En partie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="../css/play_quiz.css">
    <link rel="icon" href="../ressource/logo_quizzine_petit.png" type="image/x-icon">
</head>
<body>
    <header class="d-flex justify-content-between align-items-center mb-4">
        <div class="logo">
            <img src="../ressource/logo_quizzine_réduis.png" alt="Logo" class="img-fluid">
        </div>
        <div class="profile d-flex align-items-center">
            <img src="<% if (user && user.profilePhoto) { %> <%= user.profilePhoto %> <% } else { %> https://api.dicebear.com/9.x/big-ears/svg?seed=<%= Math.random().toString(36).substring(7) %> <% } %>" alt="Profile" class="profile-img">
            <span class="status ms-2">
                <% if (user) { %>
                    <%= user.userName %>
                <% } else { %>
                    Invité
                <% } %>
            </span>
        </div>
    </header>

    <div class="container mt-5">
        <div id="quiz-container" class="quiz-container row g-4 justify-content-center"></div>

        <div class="progress mb-4" id="progress-bar-box">
            <div id="progress-bar" class="progress-bar" role="progressbar" style="width: 0%;"></div>
        </div>

        <div id="score" class="text-center mt-4 score-section"></div>

        <div class="text-center mt-4">
            <button id="submit" class="btn btn-primary submit">Soumettre les réponses</button>
        </div>
    </div>

    <div id="alert-modal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <p id="modal-message"></p>
        </div>
    </div>

    <% if (project) { %>
        <script id="project-data" type="application/json">
            <%= JSON.stringify(project) %>
        </script>
    <% } %>
    <script src="../script/project_quiz.js" defer></script>
    <div id="burger-menu"></div>
    <script src="../script/importer_burger.js" defer></script>
</body>
</html>